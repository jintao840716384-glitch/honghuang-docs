# 修改记录

## 2026-07-08：架构优化阶段收口与新对话交接页

- 新增 `下一阶段新对话交接摘要.md`，用于结束本轮框架优化后开启下一轮玩法、内容或数值设计对话。
- 交接页明确当前第一轮框架级拆分已完成，后续默认进入真实内容迭代，不再为了拆分本身继续扩大底层改动。
- 交接页整理了下一轮常见需求的默认落点、不要重复引入的旧规则、适合开始的内容任务和验证要求。
- `README.md` 文档索引加入该交接页，并补充阶段切换时需要维护该文件的规则。

## 2026-07-08：键盘快捷键重绑定 UI 接入

- `InputActionDatabase.gd` 增加动作文本 id、设置页分类、按键事件转换和绑定显示工具。
- `InputSettings.gd` 增加按当前设置读取动作绑定、显示绑定、设置单个主绑定和恢复默认的入口。
- `SettingsStore.gd` 增加单个动作绑定写入和恢复默认入口，UI 不再直接拼 `input.bindings`。
- `LocalizationDatabase.gd` 补充设置页操作区、改键、默认和全部输入动作的中英文文本。
- `MainMenuScene.gd` 设置面板新增“操作”区域，支持每个动作一个键盘主绑定、按下新键确认、恢复默认；鼠标 / 手柄绑定和冲突提示保留后续扩展。
- `PrototypeSmokeTest.gd` 增加输入绑定边界检查，覆盖按键事件转绑定、绑定显示、应用到 `InputMap` 和恢复默认。

## 2026-07-08：存档版本与迁移服务拆分

- 新增 `scripts/save/SaveMigrationService.gd`，统一维护存档类型、schema 版本、设置迁移和局外成长迁移。
- `SettingsStore.gd` 保存 `user://settings.cfg` 时写入 `_meta.save_kind` 和 `_meta.schema_version`，读取时先取版本再迁移 / 清洗设置。
- `MetaProgression.gd` 的默认数据、读取合并和保存前清洗改为调用 `SaveMigrationService.gd`，旧 `progression.json` 字段继续兼容。
- `PrototypeSmokeTest.gd` 增加存档迁移边界检查，覆盖设置元数据、设置清洗、局外成长版本升级和负数旧字段清洗。
- 架构约束和数据规划补充存档版本规则：后续改设置、局外成长、收藏、图鉴或成就字段时，先更新 schema 和迁移服务，不在 UI 或运行流程里临时修旧数据。

## 2026-07-08：商业化基础设置层占位拆分

- 新增 `scripts/settings/GameSettings.gd`，统一显示、音频、玩法、辅助功能和输入设置的默认值与清洗规则。
- 新增 `scripts/settings/SettingsStore.gd`，统一从 `user://settings.cfg` 读取 / 保存设置；主菜单音频滑条和显示选项已改为通过该入口保存。
- 新增 `scripts/settings/DisplayModeManager.gd`，固定 16:9 分辨率档、窗口模式、垂直同步和等比留黑策略；`project.godot` 固定 1920x1080 基准、关闭随意拖动窗口大小，并保留等比缩放。
- 新增 `scripts/settings/InputActionDatabase.gd` 与 `scripts/settings/InputSettings.gd`，集中维护输入动作、默认快捷键和玩家绑定应用；主流程启动时统一应用输入设置。
- 新增 `scripts/audio/MusicManager.gd`，按 `AudioEventDatabase.gd` 中的音乐事件管理背景音乐播放、循环和音乐音量；主菜单 / 地图和战斗已接入音乐事件占位，主菜单音乐音量滑条会同步到主流程音乐管理器。
- `MainMenuScene.gd` 的设置面板扩为最小音频 / 显示设置 UI，支持主音量、音乐音量、音效音量、分辨率、窗口模式和垂直同步；设置变更通过信号交给 `Main.gd` 应用，不让单个按钮绕过设置层。
- `AudioManager.gd` 增加音效音量入口，所有新建音效播放器会读取 `SettingsStore.gd` 中的 `sfx_volume`。
- 新增 `scripts/assets/AssetAttributionDatabase.gd`，记录素材来源、作者、授权和商用许可，作为后续导入 Steam 商用素材前的登记入口。
- `PrototypeSmokeTest.gd` 增加商业化基础层边界检查，覆盖设置清洗、显示档位、输入动作、音乐事件和素材授权登记。

## 2026-07-08：文本本地化底座拆分

- 新增 `scripts/localization/LocalizationDatabase.gd`，统一维护语言列表、文本 id、中文 / 英文文本和缺失回退。
- 新增 `scripts/localization/LocalizationService.gd`，提供按当前设置读取文本、参数替换和语言 id 解析。
- `GameSettings.gd` 增加 `localization.language_id`，`SettingsStore.gd` 增加语言设置更新入口，语言选择会保存到 `user://settings.cfg`。
- `MainMenuScene.gd` 的主菜单和设置页第一批文案改为读取文本服务，并在设置面板加入语言选项，切换后当前界面示例文本即时刷新。
- 架构约束新增“自检范围控制”，按单点小改、同模块扩展、跨模块功能和架构拆分控制检查强度；同时把长期硬编码玩家可见文本列入禁止项。
- `PrototypeSmokeTest.gd` 增加文本本地化边界检查，覆盖默认语言、英文文本、参数替换和缺失 text id fallback。

## 2026-07-08：视觉与听觉素材系统占位拆分

- 新增 `scripts/assets/VisualAssetDatabase.gd`，按 UI 贴图、静态场景图、角色精灵、战斗动画、图标和 VFX 记录视觉资源占位 id、未来路径和占位色。
- 新增 `scripts/assets/CharacterVisualDatabase.gd`，把角色 / 敌方的视觉包装从角色基础数值中独立出来；`CharacterDatabase.gd` 只声明 `visual_profile_id` / `animation_profile`，不直接写贴图路径。
- 新增 `scripts/assets/BattlePresentationRouter.gd`，先记录战斗事件到视觉表现动作的映射，后续动画、特效和序列帧从这里扩展，不写入战斗结算。
- 新增 `scripts/audio/AudioEventDatabase.gd`，把音频事件、类别、音量、冷却、未来音频路径从 `AudioManager.gd` 中迁出；`AudioManager.gd` 继续负责播放和程序占位音效。
- 新增 `assets/visual/` 与 `assets/audio/` 素材目录说明和占位文件，后续导入素材必须先登记到对应数据库，再由 UI、角色视觉、表现路由或音频管理器引用。
- `BattleUnit.gd` 增加视觉 profile / 素材引用字段；`BattleUI.gd`、`CombatActorView.gd`、`JobSelectScene.gd` 和 `CharacterPrepScene.gd` 开始读取角色视觉 profile 的占位色，不改变现有战斗规则。
- `PrototypeSmokeTest.gd` 增加视觉 / 听觉素材边界检查，覆盖资源分类、角色视觉包装、战斗表现路由、音频事件和音乐占位事件。

## 2026-07-08：地图弹窗选择展示工厂拆分

- 新增 `MapModalChoiceFactory.gd`，统一生成探索地图弹窗中的卡牌选择网格、卡牌选项、奖励伪卡和通用操作按钮。
- `MapScene.gd` 保留事件、商店、宝箱、休息和节点完成状态提交逻辑，只把弹窗选项展示生成迁入工厂，避免探索流程继续复制卡牌 UI 生成代码。
- `PrototypeSmokeTest.gd` 增加地图弹窗选择工厂边界检查，覆盖网格、奖励项和卡牌项的基础创建。

## 2026-07-08：构筑展示工厂与战斗浮动文字规则拆分

- 新增 `DeckBuilderViewFactory.gd`，统一生成构筑两栏面板和构筑卡牌按钮；`CharacterPrepScene.gd` 与 `MapScene.gd` 保留各自保存 / 状态提交逻辑，只把重复展示生成迁入工厂。
- 新增 `BattleFloatingTextRules.gd`，把战斗事件到浮动文字、颜色和受击反馈的映射从 `BattleUI.gd` 中迁出；`BattleUI.gd` 继续负责查找场上显示对象并播放表现。
- `PrototypeSmokeTest.gd` 增加浮动文字规则边界检查；准备页和地图页移除不再需要的旧构筑卡牌预加载依赖。
- 这轮仍不拆战斗时点窗口和连锁结算，避免在规则链仍密集耦合时做高风险切割。

## 2026-07-08：通用 UI 样式工厂第一轮拆分

- 新增 `UIStyleFactory.gd`，统一生成通用面板 `StyleBoxFlat` 和按钮三态样式，保留各界面的圆角、边线宽度、留白和阴影参数。
- `BattleUI.gd`、`MapScene.gd`、`CharacterPrepScene.gd`、`JobSelectScene.gd`、`MainMenuScene.gd` 和 `RunSettlementScene.gd` 的 `_panel_style` / `_style_button` 改为薄包装，实际样式对象由 `UIStyleFactory.gd` 创建。
- 这轮只收口样式生成，不改变界面布局和业务流程；后续统一按钮、面板、弹窗视觉时优先扩展 `UIStyleFactory.gd`。

## 2026-07-08：卡牌展示规则第一轮拆分

- 新增 `CardDisplayRules.gd`，统一承接卡牌类型展示、标签展示、触发时点展示、分数读取、卡牌主体色板、出牌描边颜色和放置区槽位基础颜色。
- `CardButton.gd`、`CardTooltip.gd`、`BattleUI.gd` 的出牌描边和 `ZoneSlot.gd` 的基础槽位颜色改为读取 `CardDisplayRules.gd`，避免卡牌显示规则散落在多个 UI 文件中。
- `PrototypeSmokeTest.gd` 扩展 UI 边界检查，确认卡牌展示规则、手牌交互规则和战斗音效映射都能通过统一模块读取。
- 后续新增卡牌展示分类、tooltip 文案、卡牌色系或槽位基础颜色时，优先扩展 `CardDisplayRules.gd`；不要把展示规则写进卡牌数据表或战斗结算。

## 2026-07-08：战斗 UI 交互规则与音效映射第一轮拆分

- 新增 `CardInteractionRules.gd`，集中承接手牌按钮前缀、出牌动画前缀、拖拽落点和敌方目标需求判断；`BattleUI.gd` 保留兼容包装函数，后续新增卡牌交互规则优先扩展该模块。
- 新增 `BattleAudioRouter.gd`，把战斗事件类型到音效事件名的映射从 `BattleUI.gd` 中迁出；`BattleUI.gd` 仍负责接收战斗事件和播放节点，但不再在事件分支里硬编码音效选择。
- `PrototypeSmokeTest.gd` 增加 UI 交互规则与音效映射边界检查，覆盖防御牌、装备牌、敌方目标牌、直接使用牌和基础战斗音效事件。
- 架构约束、当前交接说明、UI 交互规范和音频反馈规范已同步新边界；动画 / 特效的独立订阅仍保留为后续表现层拆分任务。

## 2026-07-08：卡牌定义、效果步骤与行动规则第二轮细化
- 新增 `CardDefinitionDatabase.gd`，承接全部卡牌定义、卡牌类型常量和基础分数读取；后续新增普通卡牌默认落到该文件。
- `CardDatabase.gd` 缩为兼容门面，继续提供旧入口、卡牌实例 uid 和解锁元数据补齐；`CardPoolDatabase.gd` 改为直接读取 `CardDefinitionDatabase.gd`，避免卡池规则反向依赖旧门面。
- `EffectResolver.gd` 的通用步骤执行现在可返回伤害 / 治疗结果；`CardResolver.gd` 的指定目标伤害和随机敌方伤害改走统一 `damage` 步骤，保留原有出牌流程和日志。
- 新增 `BattleActionRules.gd`，先承接普通攻击目标、普通攻击伤害、防御加成和攻击修正清理；`BattleManager.gd` 仍保留时点窗口、敌方行动执行和胜负推进。
- `PrototypeSmokeTest.gd` 增加卡牌定义库边界检查；架构约束、数据规划和当前交接说明已同步新模块边界。

## 2026-07-08：架构约束文档升级为长期执行协议
- `架构拆分与商业化迭代约束.md` 增加默认工作协议，明确后续新增功能、内容、UI、规则、动画、音频或系统时，即使用户未特别提醒，也要先判断模块归属和架构风险。
- 新增数据与执行分离原则：数据系统描述事实和配置，规则 / 执行系统解释数据，UI 和表现层只展示、提交请求或响应事件。
- 新增常见需求落点、全系统可复用能力扩展规则、变更等级和架构债务清单，用于约束后续迭代不要为单个需求新建长期平行系统。
- 强化每次迭代前后检查和禁止清单，要求临时特例必须主动说明、记录最终归属，并优先扩展现有规则组或数据源。

## 2026-07-08：卡牌获得落点规则拆分
- 新增 `CardAcquisitionRules.gd`，统一处理获得卡牌后的合法性检查、进入当前卡组或备牌区、结果消息。
- `RewardService.gd` 保留奖励来源、奖励数值和旧兼容入口；`card_gain_result(...)` 与 `reward_card_result(...)` 改为转发到 `CardAcquisitionRules.gd`。
- `ShopService.gd`、`EventService.gd`、`RunState.gd` 和 `BattleManager.gd` 的入卡去向判断改为调用 `CardAcquisitionRules.gd`，避免奖励、商店和事件各维护一套落点规则。
- `PrototypeSmokeTest.gd` 新增入卡规则边界检查，确认可入当前卡组、转入备牌区和旧兼容入口行为一致。

## 2026-07-08：局外成长定义数据库拆分
- 新增 `ProgressionDatabase.gd`，承接成长项定义、价格、分类、加成字段、称号阈值和层倍率。
- `MetaProgression.gd` 保留职业修为点、成长等级、购买逻辑、加成汇总和 `user://progression.json` 存档读写；旧静态定义入口改为兼容转发。
- `ExplorationRules.gd` 和 `RunState.gd` 的层倍率读取改为调用 `ProgressionDatabase.gd`，不再为了规则常量依赖存档类。
- `CharacterPrepScene.gd` 的成长项展示改为读取 `ProgressionDatabase.gd`，购买仍通过 `MetaProgression.gd`。
- `PrototypeSmokeTest.gd` 新增成长定义边界检查，确认称号、层倍率、职业成长定义和旧兼容入口保持一致。

## 2026-07-08：战斗遭遇运行期适配拆分
- 新增 `BattleEncounterRuntime.gd`，承接战斗运行期需要的敌方遭遇选择、主敌人数据、敌方牌组 profile、敌方抽牌数和出牌额度。
- `BattleManager.gd` 不再直接 preload / 查询 `EnemyDatabase.gd`，而是通过 `BattleEncounterRuntime.gd` 消费 `EncounterFactory.gd` 组装后的遭遇数据。
- `EnemyDatabase.gd` 继续作为兼容门面，保留旧入口给兼容测试和旧调用方使用。
- `PrototypeSmokeTest.gd` 新增运行期适配边界检查，确认敌方牌组、抽牌数、出牌额度和 fallback 主敌人数据保持可用。

## 2026-07-08：卡池数据库第一轮拆分
- 新增 `CardPoolDatabase.gd`，承接编号卡包、职业卡池、通用卡池、奖励池过滤、按分数随机取卡、商店库存、商店买卖价格等卡池来源规则。
- `RewardService.gd` 的战斗奖励、宝箱 / 事件卡牌来源改为调用 `CardPoolDatabase.gd`，不再直接从 `CardDatabase.gd` 取奖励池。
- `ShopService.gd` 的坊市库存和出售价格改为调用 `CardPoolDatabase.gd`。
- `RunState.gd`、`CharacterPrepScene.gd` 和平衡探针的卡包展示 / 开局备选池查询改为调用 `CardPoolDatabase.gd`。
- `CardDatabase.gd` 暂时保留卡牌定义和旧兼容入口；后续可继续把旧卡池入口改为兼容转发或移除。
- `PrototypeSmokeTest.gd` 迁移卡池检查到 `CardPoolDatabase.gd`，确认卡包、奖励池和价格行为保持一致。

## 2026-07-08：战斗启动参数入口收敛
- `RunState.gd` 新增 `battle_start_payload(...)`，统一检查节点可用性、战斗进入阻塞原因、待处理节点记录，并返回战斗场景需要的职业、卡组、战斗编号、遭遇类型、本轮上下文和灵石奖励。
- `Main.gd` 的地图节点点击回调改为读取 `RunState` 的战斗启动 payload，再创建战斗场景；应用层不再直接组装战斗参数。
- `PrototypeSmokeTest.gd` 扩展探索战斗检查，覆盖战斗启动 payload 和待处理节点记录。
- 同步更新架构约束、数据配置规划和当前交接说明；`Main.gd` 当前保留场景切换、信号连接和返回流向判断。

## 2026-07-08：战斗回流与探索流程入口收敛
- `RunState.gd` 新增 `apply_battle_completion(...)`，统一提交战斗后的卡组、备牌、生命、灵石奖励、节点完成、首领层间推进和最终探索结算。
- `RunState.gd` 新增 `abandon_run(...)`，中途放弃 / 战败结算统一走探索状态入口。
- `Main.gd` 的战斗完成回调改为只读取 `RunState` 返回的 `flow`，再切回地图或进入结算页，不再直接拼接战斗奖励、节点完成、Boss 推层和结算逻辑。
- `PrototypeSmokeTest.gd` 扩展战斗回流检查，覆盖普通战斗回流、首领推进下一层和第三层首领结算。
- 同步更新架构约束、数据配置规划和当前交接说明；应用层仍负责场景切换和信号连接。

## 2026-07-08：探索运行规则服务第一轮收敛
- 新增 `ExplorationRules.gd`，集中承接节点修为奖励、节点完成收益、休息恢复结果和探索通关 / 失败结算数值。
- `RunState.gd` 保留探索状态、局外成长写入和兼容入口，节点完成、休息和结算计算改为转发给 `ExplorationRules.gd`。
- `RewardService.gd` 的节点修为奖励入口改为兼容转发给 `ExplorationRules.gd`，并新增通用奖励卡结果包装，供宝箱和事件复用。
- `MapScene.gd` 的宝箱选卡和休息按钮改为提交给 `RunState`，不再直接拼接宝箱结果或直接恢复生命。
- `PrototypeSmokeTest.gd` 扩展探索经济检查，覆盖宝箱入卡、休息恢复和节点修为规则来源。
- 同步更新架构约束、数据配置规划和当前交接说明；当时战斗回流和层间推进尚未收敛，后续已由 `RunState.apply_battle_completion(...)` 接住。

## 2026-07-08：事件规则服务第一轮收敛

- 新增 `EventService.gd`，统一生成事件种类、普通事件、罕见机缘和试炼交易的选项候选，并集中计算普通 / 奇遇 / 交易事件结果。
- `RunState.gd` 增加事件选项和事件结果薄入口，事件种类、事件选项生成、事件奖励和交易结算转发给 `EventService.gd`。
- `MapScene.gd` 的事件弹窗改为读取 `RunState` 返回的事件选项和提交事件选择，不再直接计算事件随机分支、交易代价、交易候选、最高分备牌或事件状态变更。
- 删除 `MapScene.gd` 内部未再使用的最高分备牌选择函数，避免 UI 继续保留事件规则。
- `PrototypeSmokeTest.gd` 扩展探索经济检查，覆盖普通事件选项、交易事件选项生成和事件结果应用。
- 同步更新架构约束、数据配置规划和当前交接说明；当时宝箱、休息和探索结算尚未服务化，后续已由 `ExplorationRules.gd` 接住。

## 2026-07-08：商店规则服务第一轮收敛

- 新增 `ShopService.gd`，统一提供坊市库存、刷新费用、购买和出售规则。
- `RunState.gd` 增加商店薄入口，负责提交灵石、卡组和备牌状态变化，具体规则转发给 `ShopService.gd`。
- `MapScene.gd` 的坊市刷新、购买和出售改为调用 `RunState` 商店入口，不再直接扣灵石、翻倍刷新费用或移除备牌。
- `PrototypeSmokeTest.gd` 扩展探索经济检查，覆盖坊市刷新、购买和出售的状态变化。
- 同步更新架构约束、数据配置规划和当前交接说明；事件选项和事件代价收益仍待后续 `EventService` 拆分。

## 2026-07-08：奖励规则服务第一轮收敛

- 新增 `RewardService.gd`，统一提供战斗奖励候选、宝箱 / 事件卡牌来源、事件灵石奖励、节点修为奖励和奖励卡进入当前卡组或备牌区的判断。
- `RunState.gd` 保留探索状态和状态提交，奖励相关函数改为转发 `RewardService.gd` 后再修改卡组、备牌或灵石。
- `BattleManager.gd` 的胜利三选一生成和奖励入卡去向改为调用 `RewardService.gd`，不再直接查询奖励池或构筑阻塞原因。
- `PrototypeSmokeTest.gd` 增加奖励服务边界检查，确认 `RunState` 的奖励数值与服务结果一致。
- 同步更新架构约束、数据配置规划和当前交接说明；商店库存、出售和事件选项仍留给后续 `ShopService` / `EventService` 拆分。

## 2026-07-08：构筑规则服务第一轮收敛

- 新增 `DeckBuildRules.gd`，统一提供卡组张数、卡组总分上限、加卡阻塞原因和构筑合法性判断。
- `RunState.gd`、`BattleManager.gd` 的奖励入卡入口、`CharacterPrepScene.gd` 和 `MapScene.gd` 改为调用 `DeckBuildRules.gd`，不再各自维护构筑判断。
- 原型测试和常用平衡探针同步改用 `DeckBuildRules.gd`，避免正式流程和验证脚本使用两套构筑规则。
- 同步更新架构约束、数据配置规划和当前交接说明，标记阶段 4 的构筑合法性收敛已完成第一轮；奖励、商店、事件服务仍待后续拆分。

## 2026-07-08：敌方 AI 控制器第一轮抽离

- 新增 `SideController.gd` 和 `EnemyAIController.gd`，先承接敌方行动队列、下一行动选择、阵营手牌优先级和敌方主动牌 profile。
- `BattleManager.gd` 改为询问敌方 AI 控制器后再执行原有战斗时点、响应窗口和伤害结算，避免在战斗管理器里继续扩展敌方专属决策。
- `PrototypeSmokeTest.gd` 增加敌方 AI 控制器边界检查，确认敌方卡牌优先级由控制器返回。
- 同步更新架构约束和当前交接说明，标记阶段 3 已完成第一轮兼容迁移。

## 2026-07-08：职业包装引用角色池

- `JobDatabase.gd` 改为通过 `character_id` 引用 `CharacterDatabase.gd` 中的玩家主单位模板，不再重复维护剑修 / 符修的基础生命、攻击、防御和标签。
- `get_job()` 继续向外返回 `max_hp`、`attack`、`defense`、`unit_tags` 和 `start_deck` 等旧字段，保护角色准备页、探索状态和战斗入口的兼容性。
- `PrototypeSmokeTest.gd` 增加职业包装引用角色池的检查，确认职业基础属性来自统一角色池。
- 同步更新架构约束、数据配置规划和当前交接说明，标记阶段 2 已完成第一轮兼容迁移。

## 2026-07-08：敌方遭遇底座文件拆分

- 新增 `CharacterDatabase.gd`、`EnemyDeckTemplateDatabase.gd`、`DifficultyCurveDatabase.gd` 和 `EncounterFactory.gd`，分别承载通用角色池、敌方卡组模板、难度曲线和遭遇组装。
- `EnemyDatabase.gd` 缩为兼容门面，保留 `character_template()`、`deck_templates_for()`、`get_encounter_for_battle()` 和 `get_enemy_for_battle()` 旧入口，调用方暂不需要同步迁移。
- 同步更新架构约束、数据配置规划和当前交接说明，标记阶段 1 已完成第一轮行为不变的文件拆分。

## 2026-07-08：架构拆分约束文档

- 新增 `架构拆分与商业化迭代约束.md`，作为后续功能迭代和架构重构前必须对照的常驻约束文档。
- 文档明确战斗核心、内容数据、遭遇组装、探索流程、局外成长、UI、动画、音频和存档的目标边界，强调玩家和敌方共用统一单位、阵营、卡牌和效果系统。
- 文档记录当前过渡混杂点、分阶段重构任务表、每次迭代前后检查清单和禁止清单，用于避免重复系统、死代码和平行规则继续扩大。
- `README.md` 和 `备份与文档维护规范.md` 已接入该文档，后续改变系统边界、依赖方向或长期重构路线时需要同步更新。

## 2026-07-08：角色池、敌方卡组模板与难度曲线底座

- `EnemyDatabase.gd` 从旧的单一敌人轮换改为三段结构：通用角色池、敌方卡组模板池和难度曲线。
- 角色池开始同时收纳玩家可选角色基础模板、普通敌人、精英 / 首领候选和未来召唤物可引用单位；玩家局外成长仍由 `JobDatabase` / `MetaProgression` 包装，不直接写进角色池。
- 敌方生成顺序固定为：主线层数 + 节点位置 + 战斗类型 + 世界难度先选择敌方卡组模板；卡组模板再声明可引用的角色、站位、敌方阵营卡组、抽牌数、出牌上限和主题标签。角色模板只作为被引用的单位数据，不反向决定卡组。
- 难度曲线按层数、地图节点位置、遭遇类型和 `world_difficulty` 输出生命 / 攻击 / 防御修正；当前世界难度默认 0，只作为后续劫数入口预留。
- `BattleManager` 改为先读取敌方卡组模板生成的完整遭遇，再实例化主敌人和额外敌方单位；外部 `start_run_with_deck(...)` 参数保持兼容。
- `PrototypeSmokeTest.gd` 增加角色池、每层首领卡组模板数量、世界难度加成和遭遇单位装配检查。
- 架构迁移后回调默认世界难度 0 的模板强度：当前标准模板主要保持单敌方单位，多单位留给后续内容或更高世界难度，避免底层迁移直接抬高主线难度。
- 最新 `SwordProgressionProbe.gd` 固定种子结果：0 成长三层连续约 60.0%，两级生命约 66.3%，两级生命 +1 攻击约 83.8%，开放第二批且总分 +2 约 92.5%，三批成熟档约 88.8%，基础抽牌 +1 档为 100%。

## 2026-07-07：角色成长页分组展示

- `MetaProgression` 的成长项增加轻量 `category` 元数据，用于 UI 分组，不改变存档结构、成长数值或购买逻辑。
- 角色准备页的“角色成长”现在显示称号、可用 / 累计修为点、当前生命、攻防、基础抽牌和卡组总分上限。
- 成长项按“基础属性 / 卡组与卡包 / 角色特性 / 强力规则”分组展示，并在说明中直接显示下级消耗和修为点是否足够。
- `RunFlowSmokeTest.gd` 增加成长页标题、资源摘要和基础属性分组检查。

## 2026-07-07：角色准备页开局构筑

- 角色准备页按钮从“初始牌组”调整为“开局构筑”，进入后可在当前卡组和备选池之间移动卡牌。
- 开局构筑使用“职业默认开局牌 + 当前已开放编号卡包每卡 1 张”的临时备选池；默认开局牌中未开放卡包的特例牌仍作为开局牌保留，但不会额外进入奖励池。
- 开始挑战会把准备页当前卡组传入 `RunState`；准备页备选池不作为探索备牌带入，避免未选卡开局即可在坊市出售。
- 如果构筑不满足 10-20 张或卡组总分上限，会阻止开始挑战。
- 当前不做永久收藏、卡牌库存和同名上限；这一步只用于让玩家在正式进入探索前调整本轮开局卡组。
- `RunFlowSmokeTest.gd` 增加开局构筑页容器和开始挑战信号中的卡组 / 备选池检查。

## 2026-07-07：卡包解锁入口强化

- 角色准备页按钮从“卡包状态”调整为“卡包解锁”，不再只是只读展示。
- 卡包解锁页继续列出通用 1-5 和当前角色职业 1-5 的槽位、开放状态和已有卡数，同时显示当前识藏等级、可用修为点和下一批卡包。
- 玩家现在可以直接在卡包解锁页消耗修为点提升“识藏”，开放下一批编号卡包；卡包过滤规则仍只影响奖励、事件、宝箱和坊市，不影响起始牌组。
- `RunFlowSmokeTest.gd` 增加卡包解锁页标题和按钮行检查。

## 2026-07-07：显式编号卡包名单与分档复测

- `CardDatabase` 新增 `CARD_UNLOCK_PACKS`，当前已实现卡牌不再只依赖旧 `unlock_tier` 自动推导包名；旧 `CARD_UNLOCK_TIERS` 仍保留为兼容 fallback。
- 当前显式包表覆盖 `common_1 / common_2 / common_3`、`sword_1 / sword_2 / sword_3`、`talisman_1 / talisman_2`；4、5 号包继续作为 UI 槽位预留，暂不放入正式奖励池。
- `PrototypeSmokeTest.gd` 增加包槽位与典型卡归属检查：例如 `火球符 -> common_1`、`雷击符 -> common_2`、`护心镜 -> common_3`、`一剑开天 -> sword_3`。
- 最新 `SwordProgressionProbe.gd` 固定种子结果：`pack_tier 0` 奖励池为普通 16、精英 7、Boss 7；0 成长三层连续 57/80，约 71.3%；两级生命约 63.8%；两级生命 +1 攻击约 86.3%；开放第二批且总分 +2 约 88.8%；三批成熟档约 90.0%；基础抽牌 +1 档为 100%。

## 2026-07-07：探索结算页与角色选择文案

- 新增 `RunSettlementScene`，探索失败和第三层首领胜利都会先进入结算页，不再直接停留在战斗界面、返回主菜单或回到空地图。
- 结算页展示探索结果、到达层数、完成节点数、基础修为、层数倍率、本次获得修为点，以及当前角色可用 / 累计修为点。
- 结算页提供“返回角色界面”和“再次挑战”两个入口；再次挑战会沿用当前角色重新开始一轮探索。
- 新增 `CharacterPrepScene`：选择角色后先进入角色准备页，再由准备页开始挑战；准备页提供角色成长、卡包解锁、开局构筑和返回角色选择。
- `CardDatabase` 增加卡包槽位接口，准备页“卡包解锁”会列出当前角色相关的通用 1-5 和职业 1-5，显示开放状态和已有卡数；未开放卡包不会进入正式奖励池。
- 玩家可见的选择页文案从“选择职业 / 职业成长”改为“选择角色 / 角色成长”，局外成长入口从选择页迁移到角色准备页；底层 `job_id` 和存档结构暂不改名，避免破坏已有数据。
- `RunFlowSmokeTest.gd` 增加结算页和角色准备页实例化、基础字段和按钮信号检查。

## 2026-07-07：GitHub 仓库备份规则更新

- GitHub 仓库恢复为项目主备份和版本记录方式；日常“保存稳定版本”默认整理改动、提交并推送到 `origin/main`。
- 本地压缩包降级为高风险操作前的额外保险，不再作为普通 Demo 改动的常规备份。
- 明确仓库还承担历史对比、按文件恢复、重构前存档点、分支实验和后续阶段版本管理等作用。
- 验证边界调整为：Git 用于确认版本记录和提交范围，游戏功能正确性仍通过定向搜索、Godot 无头检查和必要手测判断。

## 2026-07-07：成长分档探针与低卡包奖励池修正

- 新增 `SwordProgressionProbe.gd`，按局外成长阶段测试三层连续路线：0 成长、两级生命、生命 + 攻击、开放第二批卡包、成熟三批卡包、基础抽牌 +1 等档位。
- 探针会先输出当前剑修编号卡包的奖励池规模，并检查起始牌组中哪些牌不在 `pack_tier 0` 奖励池内；当前 `小无相剑` 是起始牌组特例，不会从第一批奖励池随机出现。
- 修正低卡包阶段 Boss 奖励候选过窄的问题：按遭遇类型和分数过滤后如果候选少于 3 张，会在同一已解锁卡包范围内放宽分数下限，不会放开未解锁卡包。
- 当前固定种子成长分档探针结果：`pack_tier 0` 奖励池为普通 16、精英 8、Boss 8；0 成长三层连续约 61.3%，两级生命约 63.8%，两级生命 +1 攻击约 90.0%，基础抽牌 +1 档为 100%。
- 后续判断难度时要同时看“无局外成长”“局外成长分档”“当前卡包深度”和“模拟玩家水平”。简单自动出牌和较聪明自动出牌的结果差异较大，不应只看单一探针。

## 2026-07-07：失败结算与局外成长价格重标定

- 探索战败返回菜单前会结算本轮已完成节点的基础修为点；未完成的当前战斗节点不计入收益。
- `RunState.finish_run_and_save_progression()` 增加通关 / 失败结算标记，重复调用不会重复发放修为点。
- 三层制下重新抬高局外成长价格：生命、攻击、卡组总分、识藏、基础抽牌、防御和剑修开局剑势都改为更适合几十到百点一轮收益的价格区间。
- 增加 `_test_failed_run_settlement`，覆盖“中途失败也入账且不会重复入账”的回归检查。
- 最新探针：三层连续无局外成长约为剑修初始牌组 35%、通用基础卡组 47.5%；单层探针中第一、二层 100% 通过，第三层约 32%-37% 通过。

## 2026-07-07：三层主线压力探针与第一层 Boss 微调

- 新增 `SwordThreeLayerRunProbe.gd`，模拟无局外成长的三层连续主线：卡组、备牌和奖励继承，层间回血，用于后续主线难度调参。
- 第一层 Boss 牌包加入少量直接压力和虚弱：从纯 `铁木甲 / 破甲符` 改为 `铁木甲 / 破甲符 / 缚身符 / 金刃符` 轻牌包。
- 第一层普通和精英仍不配置敌方卡组；第一层定位从“几乎无伤教学”调整为“低压教学但会扣血”。
- 当前探针基线：第一层单层仍基本 100% 通过但会扣血；无局外成长三层连续通关率约为剑修初始牌组 35%、通用基础卡组 47.5%，失败集中在第三层末段。

## 2026-07-07：编号卡包兼容层

- `CardDatabase` 接入编号卡包过滤：默认 `card_unlock_tier = 0` 会开放 `common_1` 和当前职业的 `职业_1`，后续层级依次开放 `common_2 / 职业_2`、`common_3 / 职业_3`。
- 卡牌元数据保留旧 `unlock_tier`，同时补齐 `unlock_pack` 和 `unlock_packs`；当前 `unlock_tier` 只作为旧存档和旧成长项的兼容层。
- 奖励、事件、宝箱和坊市仍走原有统一入口，但内部已经按编号卡包判断可出现卡牌。
- 地图状态面板的加成摘要从“卡池层级”改为显示已开放卡包，例如 `通用1、剑修1`。
- 职业成长项 `识藏` 的文案改为“开放下一批编号卡包”，保存字段暂不改名，避免破坏已有存档。
- 增加回归测试，覆盖默认卡包、第三批剑修卡包和高包卡 `一剑开天` 的解锁过滤。

## 2026-07-07：三层主线骨架第一阶段

- 标准探索结构改为三层主线：第一、二层首领胜利后刷新下一层地图，第三层首领胜利后才结算本轮修为点。
- 层间推进只刷新地图、提升敌方层级参数并回复生命，不提供局内境界属性成长，避免旧的 Boss 后突破系统回流。
- 地图 UI 增加“第 X / 3 层”表达，原“地图层数”改为“节点排数”，区分主线大层和单张地图内部排数。
- 修为点倍率按最终到达层结算；当前三层主线保留第一层 x1、第二层 x1.25、第三层 x1.5 的倍率。
- 隐藏 Boss、完整牌手对手、Boss 连战和劫数明确为通关后系统，不进入普通三层主线。
- 编号卡包方向写入文档：后续优先使用 `common_1`、`common_2`、`sword_1` 等包名，不用基础 / 进阶 / 高阶暗示强弱。

## 2026-07-07：早期难度曲线二次回调

- 第二、三层敌方基础成长重新压低：第二层主要增加少量生命和攻击，第三层开始明显抬高生命并接入普通敌方牌包，但不再让普通敌人过早获得防御。
- 精英 / 首领的额外防御继续延后到后续劫数或通关后挑战中出现，前三层先让低分卡和基础攻击保持有效，避免早期剑修和通用低分卡被防御硬卡。
- 第三层普通 / 精英敌方牌包减少直接伤害密度，改为以 `破甲符`、`缚身符`、`铁木甲` 和少量 `金刃符` 表达压力。
- 第三层首领牌包暂不使用 `雷击符` 这类大额直接伤害，改为削弱 / 装备 / 少量伤害；强力敌方攻击牌等敌方延迟或放置牌机制成熟后再回到中低层 Boss。
- 无局外成长压力测试结果从“第三层 0% 通过”回调为后续继续观察目标；第三层仍应明显变难，但不应成为纯数值硬墙。

## 2026-07-07：敌方阵营牌手与卡组模板接入

- 游戏本体接入单位稳定标签：我方主单位带 `主单位` 和职业标签，敌方单位带 `敌人`、阶级和首领 / 精英等标签；后续职业卡和目标限制可基于这些字段实现。
- 我方主单位的规则 id 改为职业 id，显示名保留职业名；不再把规则身份绑定到“玩家”显示名。
- 敌方卡牌流程从“单位行动前出牌”改为“敌方阵营牌手回合开始统一抽牌、统一出牌，然后再驱动场上单位基础行动”。
- 敌方非终极首领起始手牌为 0；普通遭遇每敌方回合抽 1，精英抽 2，首领抽 3。
- 敌方卡组模板改为按层级和遭遇等级选择：普通 / 精英可从若干固定模板中随机选 1 套，Boss 使用该层专属模板。
- 增加回归测试，覆盖敌方阵营出牌不会因为场上有多个敌方单位而变成每单位各出 1 张。

## 2026-07-07：阵营牌手与单位概念文档校正

- 明确战斗抽象只有我方阵营和敌方阵营两个牌手；场上单位不是独立牌手，没有自己的完整回合、牌库或卡组。
- 明确单位只是阵营控制的战斗对象，承载生命、攻击、防御、装备、状态、资源和基础行动额度。
- 明确失败条件差异：我方主单位死亡则战败；敌方场上单位全部死亡则胜利。
- 明确职业本质是开局默认主单位和卡池解锁来源；剑修、符修等职业卡后续应通过 `unit_id`、`unit_tags` 等稳定字段限制目标或使用者。
- 明确卡牌检索和字段解锁只看数据字段，不扫描卡面效果说明文本；文档中旧“玩家”描述后续要迁移为“我方主单位”或“我方阵营”。

## 2026-07-07：前三层敌人数值曲线与敌方牌包第一版

- 敌人同层成长改为更轻：基础敌人随节点只小幅增加生命和攻击，避免同一层内普通节点强度突兀抬升。
- 战斗难度改为按大层表驱动：第二层、第三层会整体增加敌方基础生命和攻击，防御属性继续延后并保持稀有。
- 精英和首领的额外加成改为在层级表基础上叠加；前三层防御仍基本为 0，更明显防御留给后续劫数或通关后挑战。
- 敌方卡组第一版接入后台牌区；后续已改为敌方阵营牌手回合开始统一抽牌、统一出牌，再驱动场上单位基础行动。
- 第一层普通和精英仍无敌方卡组；第一层首领使用 `铁木甲 / 破甲符 / 裂石符`，每回合抽 1 张。
- 第二层精英开始使用 `金刃符 / 破甲符 / 铁木甲` 轻牌包；第二层首领使用 `火球符 / 金刃符 / 破甲符 / 缚身符`，每回合抽 2 张。
- 第三层首领使用 `雷击符 / 裂石符 / 破甲符 / 缚身符 / 铁木甲`，先用主动伤害和削弱表达压迫感，暂不启用敌方放置牌。

## 2026-07-07：第一版局外成长与第一层降难

- 第一层敌人数值整体下调：普通敌人的基础生命、攻击和同层成长放缓，精英 / 首领额外数值加成降低，第一层防御仍基本为 0。
- 旧记录：当时敌方牌库暂为空；后续已补第一版首领 / 精英轻牌包。
- Boss 胜利后不再调用旧的局内境界突破流程；本轮探索完成后结算修为点并结束当前地图。
- 新增 `MetaProgression.gd`，将职业局外成长保存到 `user://progression.json`，剑修和符修修为点不共享。
- 职业成长入口接入职业选择界面，可用修为点购买最大生命、攻击、防御、卡组总分上限、基础抽牌、卡池解锁层级和剑修开局剑势等升级。
- 探索节点开始记录本轮基础修为：普通 3，事件 / 坊市 / 休息 2，宝箱 4，精英 5，首领 10；结算时按大层倍率写入当前职业。
- 旧记录：卡牌数据增加解锁元数据入口 `unlock_tier`、`unlock_pack`、`default_unlocked`；当时奖励、事件、宝箱和坊市按当前职业卡池层级过滤，后续已切换为编号卡包兼容层。
- 地图状态面板改为显示称号、本轮修为和局外加成；称号保留练气 / 筑基等境界词，但不再作为局内每层突破系统。

## 2026-07-07：第一批通用卡组扩展落地

- 新增第一批通用卡：`小还丹`、`固元符`、`换气符`、`破甲符`、`缚身符`、`引路符`、`清囊术`、`攻击无效符`、`折光符`、`护阵符`、`反震符`、`封藏符`、`凝神香`、`护法灯`、`伏雷符`、`锁妖符`、`木偶侍`、`纸甲兵`、`玄铁剑`、`护心镜`。
- 新增通用底座能力：破甲、虚弱、灼伤、迟滞、下次减伤等状态计数器结算；明牌放置牌倒计时；放置牌压牌；放置区自动减伤；下一名敌方行动单位状态影响。
- 新增通用选择流程：按标签和分数筛选卡组检索、弃牌后抽牌、抽牌后弃牌、从卡组选择牌压到放置牌下方。
- 装备支持复合效果与濒死保护，`护心镜` 可破坏自身使致命伤害改为保留 1 点生命。
- 剑修初始牌组调整为只保留 `青锋剑` 一件装备，并将旧 `铁木甲`、`养剑匣`、`火球符` 替换为 `回春符`、`疾剑诀`、`金刃符`。

## 2026-07-07：项目清理起点

- 新增 `当前卡牌清单.md`，按通用卡池、剑修职业池、符修职业池和子类整理当前卡牌。
- 删除旧 `ResponsePanel` 响应弹窗文件；当前盖伏响应只使用战斗场景内的覆盖层、场上发光槽位和确认面板。
- 移除 `BattleUI` 中旧的隐藏 `UnitActionPanel`，只保留单位卡下方的内联“攻击 / 防御”按钮。
- 删除卡牌库中未使用的同名上限死代码；当前卡组约束只保留张数和卡组总分。
- 卡组调整界面按钮文案改为“退出调整”，未保存提示改为“当前卡组调整尚未保存”。
- 主文档重写为当前阶段准则，不再保留早期历史方案和过期记录。

## 2026-07-07：第一版卡牌重做落地

- 将练气卡组总分上限从 30 调整为 20，后续境界上限调整为 28、36、44、52、60。
- 重做剑修职业池：剑修牌改为围绕玩家本体普通攻击、剑势、成长、额外攻击、群体攻击、随机多段、反击和下一次攻击倍率。
- 移除当前卡池中的旧 `破甲符`、`剑心通明`、`聚灵佩` 引用；新增 `养剑匣`、`疾剑诀`、`剑气横扫`、`万剑回风`、`召剑诀`、`万剑诀` 等剑修牌。
- 扩充通用卡池的低中分伤害、回复、抽牌、盖伏反制和装备牌；当前不做五行或属性克制规则。
- 同步剑修和符修初始牌组，保证初始 10 张卡且卡组总分低于练气上限。

## 2026-07-07：防御规则调整

- 伤害步骤默认改为受目标防御力减免；只有显式配置 `ignore_defense = true` 的效果才无视防御。
- 通用符牌伤害不再写“直接伤害”，当前火球符、金刃符、雷击符等都会被目标防御减免。
- 当前剑修和符修初始防御力改为 0。
- 我方单位执行防御时，如果当前防御力为 0，则临时获得 1 点防御；否则继续按当前防御力增加等量防御。
- 低层敌人的基础防御调低为 0，防御成长放慢，精英和首领的额外防御从后续境界再开始出现。
- 旧记录：曾记录首领胜利后的突破升级方向；该方向已在后续改为局外修为点和职业成长。

## 2026-07-07：同归于尽与 0 血探索修正

- 修正反击同归于尽边界：同一结算中玩家主角和敌人同时归零时，玩家死亡优先，进入战败而不是奖励。
- 战斗从探索状态继承生命时允许继承 0；如果被外部强行以 0 生命进入战斗，战斗会直接进入失败态。
- 探索状态生命为 0 时不再返回可选节点，并阻止进入战斗，避免 0 血靠非战斗节点继续推进。
- 增加对应回归测试，覆盖“藏锋反击杀敌但玩家被原始伤害击败”和“0 血不能开战”。

## 当前稳定能力快照

- 探索地图包含战斗、精英、首领、事件、宝箱、坊市和休息节点。
- 当前卡组与备牌区分离，卡组保存范围为 10 到 20 张，并受卡组总分限制。
- 战斗支持每边最多 3 个单位，阵营回合制行动。
- 我方单位各自拥有一次基础攻击或防御。
- 所有单位拥有装备槽，装备归属、替换和攻防加成按目标单位结算。
- 盖伏响应使用“发动盖伏卡”界面和中上方结果预览。
- 手牌支持点击和拖拽两套操作，拖拽仍调用现有结算流程。
- 抽卡有从卡组飞入手牌的揭示动画，播放期间锁定操作。

## 待处理

- 重新设计剑修、符修低分卡和职业核心牌。
- 设计装备、盖伏、召唤等第一批内容牌。
- 设计低层普通敌人、精英敌人和少量敌方卡组。
- 用真实内容检验敌方后台牌区、目标规则和效果步骤还缺什么。
